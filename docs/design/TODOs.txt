Functionality:
	
* Translation via MacOS local API.
	French test page: gemini://ploum.net/2026-08-03-offpunk_manifesto.gmi
	Always Translate Content option?
	

* Themes as CSS. User supplied themes


* More Tabs
	Safari has a ton of option on close tab, close tab to the right, close all other tabs, etc
	
	
* need a way to set Major Tom as the default for different protocols


* Titan Support
* Unified history

* Import From Browser 
	Looks like it detects installed apps (Safari shows Firefox)
	Goes to a model where you check what you want to import (History, Bookmarks)

* Add a TOC/Outline view, using headings
	Maybe as a floating/collapible side panel?

* add that capsule structural/tree view 


* store cache internally as WARC? export it?
* offline mode?

* subscribe to page?
* feeds view?
* convert ATOM/RSS to gemlog page
* Viewing text content make it better
* viewing non-text content, what to do?
* view source: on binary file show a hex editor?
* Detecting Ordered Lists
* Missing "Close Tab" from File menu

* A pixelated astronaut, like Chrome's dino?
	I could use him for different error messages, with different expressions
	I could have a fun game on no internet connection
		side scrolling infinite runner, on a planet? duck UFOs, jump of rocks?


Reworking/Bugs:
	
* When restoring location, there is a flash of a new window, then it disappears and the old session is restored. Check if restoring an old sesssion before showing anything

* clicking on "Manged client certs" if a browser window is not in focus (e.g. looking at settings, or probably about) doesn't work.
	
* option "up" doesn't work. Only optin +_ shift + up. One time, in a window even the menu items didn't work to "go up". I think we should override and remove the "option" + up
	
* navgiation buttons don't look like Safari. 
	Squarcul instead of round? Do grouping like Forward/back button in Safari
	
* make sure all buttons have tooltips

* Accessibility audit: verify every primary browser action works with the keyboard and VoiceOver, including focus order and meaningful control labels/state.

* Should we show the favorites bar once they have added at least one bookmark? What do web browsers do?

* Double Favicon in tabs and bookmarks
	If a capsule has a favicon, and a page title starts with that favicon, it looks weird. Lets update this so 
	
* only use the hover text in the bottom right to show connections info. Have it disappear once the page is loaded
	
* Bug: Highlight text in the address bar swiping in the content area to go back changes the content area to blank, and the address bar does not change and the highlight stays there
	
* Organization of the "Quality of life improvements" settings is a bit rough

* Error screens are not great
* If HTTP proxy is enabled, but you can't connect to it, error page is not helpful	

* if you go to "unsupported content", and go back and forth, the content doesn't change
	Test page: gemini://jsreed5.org/about/contact/publickey.pgp


things to check:

* Verify if the Find functionality also has a "replace" option, which makes no sense in this context.

- If no windows, clicking on the icon in dock opens 2 windows (I think if no windows, I close the app right now)

- When opening a new tab, or new window, or restoring a window, the content theme is not applied until the content is downloaded and shown. So a new tab can be all white, only to then update to showing the dark theme. This is very jarring. You should show a placeholder that just shows the background color of the selected content theme, so there isn't this flash of white or black

- the image itself should be clickable, show the URL in the hover in the bottom
and it will open image in the current tab. When openning the image in a new tab, ideally it should use the cached content instead of having to redownload it. Perhaps we should be smart and use cached content for images always, for at least 30 minutes or something? what do you suggest about the caching before we implement that part
